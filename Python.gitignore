# Ignore everything in gitignore folder (files and subfolders)
/*

# Un-ignore .gitignore
!.gitignore

# Un-ignore ANY file ending in ".py" that is on the same level as the .gitignore file. (Does not track .py files in sub-folders)
!*.py
