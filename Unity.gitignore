#With meta-files enabled for Version Control in Unity, the only two directories that are
#Required are Assets and ProjectSettings. Everything else can be ignored.

*
!Assets/
!/Assets/
!ProjectSettings/
!/ProjectSettings/